import React from 'react';
import PortfolioCreditCard from '@/components/PortfolioCreditCard';

export default function EmbedCreditCard() {
  return (
    <div className="min-h-screen bg-black flex items-center justify-center p-6">
      <PortfolioCreditCard />
    </div>
  );
}
